
#include <stdio.h>
#include <string.h>

int main(){
    char language[] ="C Programming";

    printf("%s", language);

    // [strlen()] Used to find the length of a function
    printf("\nLength: %zu", strlen(language));
    return 0;
}